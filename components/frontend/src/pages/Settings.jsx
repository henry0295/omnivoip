import { Box, Typography } from '@mui/material'

const Settings = () => {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography sx={{ mt: 2 }}>System settings coming soon...</Typography>
    </Box>
  )
}

export default Settings
